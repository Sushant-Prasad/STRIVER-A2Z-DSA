class Solution {
    public int fib(int n) {
        // Base cases:
        // fib(0) = 0, fib(1) = 1
        if (n == 0 || n == 1) return n;

        // Recursive case:
        // fib(n) = fib(n-1) + fib(n-2)
        return fib(n - 1) + fib(n - 2);
    }
}
