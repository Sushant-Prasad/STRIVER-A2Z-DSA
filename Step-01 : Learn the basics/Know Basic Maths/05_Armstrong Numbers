// User function Template for Java
class Solution {
    static boolean armstrongNumber(int n) {
        // code here
       int temp = n;
       int sum = 0;
       while(temp!=0){
           int digit = temp%10;
            sum+= Math.pow(digit,3);
           temp/=10;
       }
       return sum==n;
    }
}
